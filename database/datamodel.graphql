type Team {
  id: ID! @unique
  name: String! @unique
  category: String!
  members: String!
  finalScore: Int
  createdAt: DateTime!
}

type Event {
  id: ID! @unique
  team: String!
  order: Int!
  time: String
  reps: Int
  weight: Int
  ranking: Int
  createdAt: DateTime!
}
